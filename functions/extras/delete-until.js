/**
 * Delete moments until the current move (including current move)
 */
const deleteUntil = (moments, current) => {
  if (!current) {
    return moments;
  }
  // TODO: implement actual logic
  return moments;
};

module.exports = deleteUntil;
